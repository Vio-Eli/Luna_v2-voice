// joins component to vc begins listening process
const { VoiceChannel } = require("discord.js");

module.exports = {
    action: "start",
    /**
     * 
     * @param {VoiceChannel} channel 
     */
    async start(channel) {
        fetch()
    }

};